import { Routes } from '@angular/router';


export const routes: Routes = [
    {
        path: '', //Cuando ponemos en vacio hacemos referencia a localhost:4200
        
    },
    {
        path: 'hello', //Cuando ponemos en vacio hacemos referencia a localhost:4200
        
    }
];

